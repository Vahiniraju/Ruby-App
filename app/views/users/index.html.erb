
<%= will_paginate %>

<header class="jumbotron">

  <p> Average score: <%= User.average_score %></p>
</header>

  <div class="panel panel-default">
    <div class="panel-body">
    <table class="table">
      <thead>
      <tr>
        <th>Username</th>
        <th>Score</th>
        <th></th>
      </tr>
      </thead>
      <tbody>
      <% @users.each  do |user| %>
        <tr>
          <td><%= user.username%></td>
          <td><%= user.score%></td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
  </div>

  <%= will_paginate %>
